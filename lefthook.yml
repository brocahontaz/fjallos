pre-commit:
  parallel: true
  commands:
    prettier:
      glob: "*.{ts,tsx,css,json,md}"
      run: bunx prettier --check {staged_files}
    biome:
      glob: "*.{ts,tsx}"
      run: bunx biome check {staged_files}
